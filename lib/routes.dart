import 'package:sunweather/home/home.dart';
import 'package:sunweather/settings/settings.dart';

var appRoutes = {
  '/': (context) => const HomePage(),
  '/settings': (context) => const SettingsPage(),
};